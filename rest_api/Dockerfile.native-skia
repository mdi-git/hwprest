FROM rust:latest

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        clang \
        libfontconfig1-dev \
        libfreetype6-dev \
        pkg-config \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace/rhwp
